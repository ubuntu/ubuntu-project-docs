# Phase 10 — Docs Closure

Refresh architecture docs and developer usage around the catalog-driven
template rendering pipeline.

Deliverables:
- `tools/auto-mir/README.md` with Mermaid architecture diagram
- Concise usage intent (with `--help` as the reference for detailed CLI options)
- Update any stale references in `design.md`

**Exit criteria**: README covers full pipeline; `--help` output is accurate.
